// bring in commands to access persistent database to update points

module.exports.identifyMerlin = function(memcache) {
  if (/* winner === true */) {
    // update knights' points
    // update game count of all players
  } else if (/* winner === false */) {
    // update minions' points
    // update game count of all players
  }

  return 'exit';
}