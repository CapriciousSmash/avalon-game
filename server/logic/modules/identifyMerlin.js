module.exports.identifyMerlin = function(memcache) {
  // pause for 2 minutes
    // send back countdown?

  if (/* mguess === merlin */) {
    // set winner to false(minions)
  } else {
    // set winner to true(knights)
  }

  // pause for 30 sec for gestation

  return 'gameOver';
};

module.exports.resolveMerlin = function(memcache) {

};